# hsbot 项目审计报告 · 2026-09-17

> 范围：bb087bd..1deb468 共 11 个提交（P1 性能优化、SimSnapshot 统一快照层
> 六任务、对账通道、文档立法），+1114/−139 行、20 文件。
> 方法：3 路并行深审（planner 语义 / hsbot live 面与对账 / 契约·测试·立法），
> 关键疑点均实跑验证（自建对拍 fuzz 415 例、上界暴力反例搜索 4500 例、
> 39 场景独立新旧对拍、性能数字全量复现），非纯静态阅读。
> 与 SDD 流程的任务级评审/终审相互独立——评审对照计划文本，本审计独立对照
> spec 与游戏语义。上一轮报告见 [AUDIT_2026-09-14.md](AUDIT_2026-09-14.md)。

## 零、测试基线事实

`pytest tests/`：372 passed（审计前 366 + 本审计修复波新增 6）。
三轮连跑中观察到 1 次范围外 flake：test_play_ai.py 悬浮窗用例 TclError
（约 1/3 概率，隔离跑恒绿，最后改动在 47e019a）——已立 FOLLOWUPS P5，
非本批回归。

## 一、总体结论

**本批通过审计。** spec §6 五条铁律逐条兑现（planner 依赖方向、live 输出
零变化、公共契约、零干扰、recon 零 IO）；实现方的三项关键声明均独立复现
成立：①rollout 新旧逐点等价（审计自建 fuzz 415 例零差异）；②P1 前置过滤
严格无损（上界封闭性暴力搜索 4500 例零反例）；③性能数字全部真实可复现
（58.1s→59.9s/92 局、301s→320.9s、对拍零差异）。审计发现的问题集中在
**recon 对账通道的信号噪声**（新开通道，不影响 live 主链）与**文档立法
口径**，全部已修复或入 FOLLOWUPS。

## 二、发现与处置（按严重度）

### 高（1 项，已修复 583c4ff）

1. **recon face 对账不看结算目标** — `hsbot/recon.py` + `watcher.AuditExporter`
   任何"打随从/解场"的伤害牌都产 `face 6 vs 0` 假分歧（实测复现：火球打
   敌方随从即落盘）。真实对局解场占多数，by_field 判据必被淹没。
   **修复**：capture_base 按 PLAY 块 `target` 解析"目标=敌方英雄"
   （ctrl_key==opponent 且 is_hero），仅此时 face 差才进分歧；e2e 测试
   改造+专项用例钉死。

### 中（3 项）

2. **recon 未知抽牌 hand_n/hand_cards 系统性误报**（引擎线每抽一张未知牌
   必产）——终审已登记 P4-1，本审计实测确认为首要噪声。
   **修复**（583c4ff）：实测多出的牌**全部**不在已知抽牌池 → 判"未知抽
   入手"（预测模型边界：play 对未知抽只计数）不记分歧；混合情形保守记
   真分歧。专项用例 ×2（纯未知降级/混合仍报）。
3. **mc_plan 两口径内部分叉**（planner/mc.py）——`p_lethal` 按入参
   enemy_total 裸比，`best_plan.lethal` 按统一算式折减；face=3/敌 7 的
   数值例下两字段一个 0.0 一个 True。当前零生产调用方（测试全 face=0），
   属接线 T3 时必踩的潜在地雷。**修复**（662def2）：p_lethal 与
   best_plan.lethal 统一同式，face>0 契约测试钉死。
4. **性能硬门口径未内联且不绑产量口径**（spec §5）——"≤10 分钟"未写
   orders/k_max/seed；"5 档"与血甲档位表术语相撞；orders=200 产量口径
   外推 ≈40min 超门 4 倍，且门对基线有 10× 余量（10× 回归仍可过）。
   **修复**（6d0e89a）：双门立法——探针腿（A 腿墙钟 ≤90s）+ 产量口径
   （每推演 ≤0.2s，不随语料增长漂移），术语消歧。

### 低（处置记录）

5. 无损实证脚本入库即失活（tmp_old 未入库未记载重建）→ **已修**：头注附
   一键重建命令（6d0e89a）。
6. spec §3 memo_key 写"七元组"漏 disc_next_cat（实现自始正确含之，漏进
   会撞错误坍缩）→ **已修**：spec 正名八元组（6d0e89a）。
7. Plan.enemy_total 语义漂移（rollout 启动 Plan 从折减 need 变原始档位，
   读"还差多少"会踩）→ **已修**：字段注释钉住"原始基准，need 自算"
   （662def2）；RolloutResult.snapshots 的 dealt_total 恒 0 口径同样注释
   钉住（血甲曲线=逐回合经验值，防未来消费方误读）。
8. recon mana 封顶缝（预测 min(10,·) vs 实测 mana_now 可瞬时 11）→
   **已修**：对比双侧按 10 封顶，记录保留原值。
9. recon cost_tag=None 取首个同 cid 副本（多副本错位）→ **已修**：优先
   卡表基础费副本。
10. 抉择牌按父卡 IR 预测必偏 → **已修**：evt 带 suboption 整牌不对账
    （宁漏勿错）。
11. 嵌套 PLAY 块基态泄漏（Yogg 式块内再打牌，外层 play 事件被 store 单槽
    覆写永不发 → 基态残留至局末 reset；仅内存无配对污染）→ **接受+注释
    备案**，根治需 store 覆写处发信号，列 FOLLOWUPS P4-4。
12. FOLLOWUPS 基线 364 陈旧 / 探针 docstring"93 局" / 3/425 偏差无语料
    限定 → **已修**（366→372 时点值按日期标注；92 局；比例化+复跑值）。
13. snapshots 契约用例未显式断言 launch_turn → **已修**：补
    `assert r.launch_turn == 6`。
14. tkinter 悬浮窗用例顺序性 flake → **转 FOLLOWUPS P5**（范围外）。

## 三、关键实证清单

| 审计项 | 方法 | 结果 |
|---|---|---|
| rollout 逐点等价（声明复现） | 自建旧包对拍：定向 15 例+fuzz 400 例（cap 无界 208 例逐点） | 全等，零差异 |
| 有限 cap 偏差方向 | fuzz 207 例 | hand_sizes 前缀全等，launch 只允许"更晚/None"（保守），零反向 |
| _face_ceiling 上界封闭性 | 3000 例随机构造 vs 全排列暴力 DFS + _drawable 1500 例 BFS 全树 | ceiling<可达 0 例；判闭回合手牌永不增大 |
| dealt_total=0 裁决 | 数据源语义推演（enemy_hp_curve 已含真实伤害/first_lethal_turns fresh 快照同基线）+数值例 | 裁决正确；扣减=同一份伤害计两次，启动率随回合单调高估 |
| live 输出零变化 | 旧 planner 包+旧内联 lethal_plan 复刻，39 场景逐键对拍（含 face_exp/台账/随机 30 例） | 39/39 相等 |
| recon 时序 | 真管线+合成包树六场景对抗（嵌套/批边界×2/同 eid 复打/对手交错/friendly 未解析/切回合） | 5 正确 1 泄漏（中#4，接受） |
| 零干扰 | 注入异常×2+路径占用 | 全部仅 WARNING，主链无损 |
| live 开销 | 真卡表 micro-benchmark | capture 0.18ms/块、on_play 全链 0.22ms，≈50ms/局——无需 actor 前置 |
| 公共契约 | git diff 逐字段 | Plan/RolloutResult/sim/simcal 全部完好；best_line 签名简化为 spec §3 授权 |
| 性能数字 | 探针复跑 | 59.9s/320.9s/3/460(方向恒保守)，与文档口径一致 |
| mc 口径分叉 | face=3 数值例实跑 | 修复前 0.0 vs True 实锤；修复后一致 |

## 四、遗留（全部已立案）

- FOLLOWUPS P4-3/4/5：capture 对手块观测、嵌套泄漏 store 端根治、recon
  CLI 加固（stderr/文件守卫）。
- FOLLOWUPS P5：tkinter 悬浮窗测试 flake。
- spec §3"七元组"等历史文本勘误已随手修；历史提交信息中"断言零改动"
  按行文本口径有一条例外（test_planner.py:359 表达式迁移、谓词值恒等），
  以本报告为准确记录，不改写历史。
- recon 对账通道真实对局观测期未开始（sim_divergence.jsonl 尚无实战数据）。

## 五、二轮审计（2026-09-17，范围 1deb468..70c88e6 修复波自审）

上轮修复波（6d0e89a/583c4ff/662def2/70c88e6）系控制器直接实现、未经评审，
二轮两路深审独立取证：**376 passed；双门立法/文档一致性/face 门控十个目标
枚举/mc 口径 24 格矩阵全部实证通过**，同时抓到修复波自身缺陷 2 高 3 中 8 低，
已全部修复：

- **高·吞真分歧**：recon 未知抽豁免只查实测侧多牌方向，预测侧有实测缺失
  （抽牌通道断/弃牌效应）时真分歧被静默吞掉，违反自身 docstring 承诺。
  修复：豁免前提加 `missing` 为空（after ⊇ pred）；补审计④场景用例。
- **高·归因污染**：修 6 第三级把玩家实体上**全部**附魔计入减费来源（真实
  触发源：VAC_422e 游客附魔每局挂玩家约 2 回合）。修复：render/enrich 两处
  消费侧按 `cost_downs(cid)` IR 语义过滤（store 查询保持全量并备案）。
- **中×3**：mc 主路径（非退化）公式零覆盖（突变实验证实）→ 补 face/dealt
  主路径契约用例；修 6"卡级优先"断言同名无区分力 → 卡级/玩家级独立名+
  保序断言；enrich cost 第三级零测试 → 补三级路径用例（含 actor=None）。
- **低×8**：mana diffs 条目改记原值（与 top-level 同口径）；mixed 用例改名
  正名；mc 模块头口径补全；测试注释算术勘误；watcher 重复注释；SC_755e2
  docstring 措辞（"下一张星灵牌-2"）；FOLLOWUPS 基线漂移；face 门控盲区
  （无目标随机伤害牌）与过杀钳制（预先存在）docstring 备案。

方法论注记：本轮证实"审计审计者的修复"有价值——两个高发现都是修复实现
与自身声明不一致（docstring 承诺未兑现/过滤缺失），只有独立对抗取证能抓到。
